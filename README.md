# nand2tetris-I
Build the complete Hack ALU in HDL. Fill in the ALU function table for all 18 standard operations. Verify using the ALU test script.(in 1st)
Implement NOT, AND, OR gates using only NAND gates in HDL. Draw the logic diagram for each and write the Boolean expression. Run the provided test scripts and capture the simulation output.(2nd)
Implement NOT, AND, OR gates using only NAND gates in HDL. Draw the logic diagram for each and write the Boolean expression. Run the provided test scripts and capture the simulation output.(3rd)
Implement XOR, Mux (2-way, 4-way), and DMux chips in HDL. Show truth tables and verify against the Nand2Tetris test scripts.(4th)
